## Summary

### TASK 1 (f1234568) — P1 HIGH
**File:** `backend/app/services/telemetry/pipeline.py`
**Fix:** `_store_trip_end` now orders by `Trip.start_date.asc()` to close the oldest open trip. The `WHERE user_vehicle_id` clause was already present as safety bound.
- Before: `.order_by(Trip.start_date.desc())` — closed newest → orphaned older trips
- After: `.order_by(Trip.start_date.asc())` — closes oldest correctly

### TASK 2 (f1234570) — P1 HIGH
**File:** `frontend/src/components/statistics/ChargingCurveDashboard.tsx`
**Fix:** Replaced raw `fetch()` + `localStorage.getItem("access_token")` with `statisticsApi.getChargingCurveIntegralsV2()` via centralized `apiFetch`. Enables 401 token refresh retry, CSRF handling. Removed unused `api` import.

### TASK 3 (f1234569) — P2 MEDIUM
**File:** `backend/app/api/v1/analytics.py`
**Fix:** `_get_nearest_elevation` caches results via `_elevation_cache` with key `"{lat:.4f},{lon:.4f}"`. LRU eviction at 500 entries. Repeated elevation lookups hit cache O(1) instead of O(n) SQL.

---

**QA Checklist:**
- [ ] TripEndEvent closes the oldest open trip (multi-trip scenario)
- [ ] ChargingCurveDashboard retries on 401 with refreshed token
- [ ] No raw `localStorage.getItem("access_token")` in ChargingCurveDashboard
- [ ] Repeated elevation lookups hit cache; SQL queries only on cache miss
